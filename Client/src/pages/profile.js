import React from 'react';

export default function Profile() {
  return (
    <div className="profile-page">
      {/* Intentionally blank — styles from Profile.css will apply */}
    </div>
  );
}